name: Deploy Docsify to GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # Étape 1 : Récupérer le code source
      - name: Checkout code
        uses: actions/checkout@v3

      # Étape 2 : Injecter la date de dernière modification dans les fichiers Markdown
      - name: Inject lastUpdated metadata
        run: |
          for file in $(find . -type f -name '*.md'); do
            last_modified=$(git log -1 --format=%ci -- "$file")
            if ! grep -q "^lastUpdated:" "$file"; then
              # Ajouter si absent
              sed -i "1s/^/---\nlastUpdated: $last_modified\n---\n/" "$file"
            else
              # Mettre à jour si existant
              sed -i "s/^lastUpdated:.*/lastUpdated: $last_modified/" "$file"
            fi
          done

      # Étape 3 : Configurer et installer Docsify
      - name: Install Docsify CLI
        run: npm install -g docsify-cli

      # Étape 4 : Vérifier que le site se génère correctement
      - name: Build the site
        run: docsify serve .

      # Étape 5 : Préparer pour GitHub Pages
      - name: Setup Pages
        uses: actions/configure-pages@v3

      # Étape 6 : Publier sur GitHub Pages
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v2
